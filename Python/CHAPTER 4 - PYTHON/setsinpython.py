set = {1,2,3,4}
#Sets are the collection of unordered and unique value
set1 = {1,2,2,2}

print(set)
print(set1)  # this will not print any repititive values, it will only store once 


null_set = set()  #initialse a empty set

print(type(set))

print(len(set1))  #Will ignore the duplicate values and give the length