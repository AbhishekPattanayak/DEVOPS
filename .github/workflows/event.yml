name: push_event
on:  
  push:
    branches:
        - main
jobs:
  show_info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This is a push event on all the branches"
